.env
__pycache__/
*.pyc
.venv/
*.log
.DS_Store

fotos_cache/

# Instrucciones y memoria de trabajo del proyecto: LOCAL, nunca se commitea.
# Los agentes (Claude, codex) los leen del disco igual, tracked o no.
HISTORY.md
CLAUDE.md
AGENTS.md
CASOS.md

# Shards intermedios de los harness; lo consolidado vive en eval/datos/
eval/harness/estabilidad-*.jsonl
eval/harness/inyeccion-*.jsonl

# Fotos, etiquetas individuales y respuestas de la evaluación visual.
eval/vision/private/
